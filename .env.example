# LLM provider: "mock" (default, no setup needed) or "groq" (free at console.groq.com)
LLM_PROVIDER=mock
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Reconciliation settings
AMOUNT_TOLERANCE=0.01
NUM_TRANSACTIONS=300
RANDOM_SEED=42
